using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LandController : MonoBehaviour
{
    ///<sumary>
    /// Control number of lands and determine which land should be render
    ///</sumary>
    [SerializeField] List<Land> _totalLand = new List<Land>();
    
    // Update is called once per frame
    void Update()
    {
        
    }
}
